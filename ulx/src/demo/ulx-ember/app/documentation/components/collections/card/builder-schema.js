// Reuse the same builder schema defined for the Card element docs.
// Collections/Card docs point to this file so keep it as a thin wrapper.

export { default } from '../../elements/card/builder-schema';

