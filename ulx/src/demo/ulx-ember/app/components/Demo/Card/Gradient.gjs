import { UlxCard } from 'ulx-components';

<template>
  <div class="w-400">
    <UlxCard @appearance="gradient primary" @size="s-size">
      <h6 class="bold-font mb-2">Gradient</h6>
      <p class="text-14">
        Card with a white top fading to a subtle primary layer tint at the
        bottom and a white border. Card with a white top fading to a subtle
        primary layer tint at the bottom and a white border. Card with a white
        top fading to a subtle primary layer tint at the bottom and a white
        border.
      </p>
    </UlxCard>
  </div>
</template>
