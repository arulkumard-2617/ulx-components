import React from 'react';
import { UserCard } from './user-card';

// ==========================================================================
// Basic Example
// ==========================================================================

export function BasicExample() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
      <UserCard variant="primary" size="medium">
        Primary UserCard
      </UserCard>
      
      <UserCard variant="secondary" size="small">
        Secondary Small UserCard
      </UserCard>
      
      <UserCard variant="success" size="large">
        Success Large UserCard
      </UserCard>
    </div>
  );
}
