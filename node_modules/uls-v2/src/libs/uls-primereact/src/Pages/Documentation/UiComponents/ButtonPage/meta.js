// ==========================================================================
// BUTTON COMPONENT METADATA
// ==========================================================================
// Single source of truth for Button component documentation
// This replaces hardcoded entries in DocNavItems.jsx

export default {
  // Navigation metadata
  category: 'Elements',
  subCategory: 'Button',
  menuItem: 'Button',
  routeBase: '/button',
  icon: 'pi pi-compass',
  
  // Page metadata
  header: 'Button',
  subHeader: 'Button is an extension to standard input element with icons and theming.',
  
  // Tab configuration
  tabs: [
    {
      name: 'Features',
      route: '/features',
      component: () => import('./features')
    },
    {
      name: 'Theming',
      route: '/theming',
      component: () => import('./theming')
    },
    {
      name: 'Pass Through',
      route: '/pass-through',
      component: () => import('./passThrough')
    }
  ],
  
  // Import message for the component
  importMsg: "import { Button } from 'primereact/button'",
  
  // Accessibility information
  accessibility: {
    description: "Button component renders a native button element that implicitly includes any passed prop. Text to describe the button is defined with the aria-label prop, if not present label prop is used as the value. If the button is icon only or custom templating is used, it is recommended to use aria-label so that screen readers would be able to read the element properly.",
    example: "<Button icon='pi pi-check' aria-label='Submit' />"
  }
};
