import React from 'react';
import { Space as SpaceDocs } from '@uls/foundation';
import FoundationLayout from '../Foundation/FoundationLayout';

export default function SpaceUtilitiesPage() {
  return (
    <FoundationLayout
      title="Space Utilities"
      description="Spacing scale generated from @utill.less (2px increments mapped to the global size system) with a @spacing-step of 4."
    >
      <div className='uls-messages info m-size'>
        <div role="alert" className="uls-messages info s-size enter-done" style={{ width: '850px' }}>
          <span className='fg-text'>This page references the spacing scale generated in <span className='fg-primary'>`@utill.less`</span>, where <span className='fg-primary'>`@spacing-step: 2;`</span> defines each increment.</span>
          <span className='fg-text'>Update the <span className='fg-primary'>`@spacing-step`</span> variable inside <span className='fg-primary'>`utill.overrides.less`</span> whenever your product needs a different cadence, then regenerate the utilities to keep everything in sync.</span>
        </div>
      </div>
      <SpaceDocs />
    </FoundationLayout>
  );
}
