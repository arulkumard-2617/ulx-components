import React from 'react';
import { Spacing as SpacingDocs } from '@uls/foundation';
import FoundationLayout from './FoundationLayout';

export default function SpacingPage() {
  return (
    <FoundationLayout
      title="Spacing"
      description="Quarter-rem base unit, semantic tokens, and utility mapping."
    >
      <SpacingDocs />
    </FoundationLayout>
  );
}
