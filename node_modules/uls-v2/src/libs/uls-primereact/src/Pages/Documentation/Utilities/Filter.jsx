import React from 'react';
import { FilterUtilities as FilterDocs } from '@uls/foundation';
import FoundationLayout from '../Foundation/FoundationLayout';

export default function FilterUtilitiesPage() {
  return (
    <FoundationLayout
      title="Filter Utilities"
      description="Apply blur, brightness, contrast, hue rotation, and more."
    >
      <FilterDocs />
    </FoundationLayout>
  );
}
