import React from 'react';
import { Display as DisplayDocs } from '@uls/foundation';
import FoundationLayout from '../Foundation/FoundationLayout';

export default function DisplayUtilitiesPage() {
  return (
    <FoundationLayout
      title="Display Utilities"
      description="Single-class toggles for block, inline, flex, grid, and more."
    >
      <DisplayDocs />
    </FoundationLayout>
  );
}
