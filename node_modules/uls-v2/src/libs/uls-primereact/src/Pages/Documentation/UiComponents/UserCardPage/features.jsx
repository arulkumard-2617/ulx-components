import React from 'react';
import DocPanel from '../../../../Components/Common/DocMain/DocPanel';
import CodePreview from '../../../../Components/Common/CodePreview';
import RichText from '../../../../Components/Common/RichText';

// Import your custom demos from custom-components
import { 
    BasicDemo,
    BasicSource,
} from './import';

// Define features for UserCard
export const UserCardFeatureItems = [
    {
        id: "basic",
        sectionNav: "Basic",
        sectionDesc: <RichText as="span" content="The <code>UserCard</code> component basic usage." />,
        demo: (
            <CodePreview 
                demo={<BasicDemo />} 
                source={BasicSource} 
                snippetName="basic" 
                language="jsx" 
            />
        ),
    },
];

// Export component for direct use
export default function UserCardFeatures() {
    return (
        <DocPanel 
            Header="UserCard" 
            SubHeader="UserCard is a ULS component." 
            features={UserCardFeatureItems}
        />
    );
}