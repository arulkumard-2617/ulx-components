import React from 'react';

export default function FoundationLayout({ title, description, children }) {
  return (
    <div className="doc-foundation-page pdx20">
      <header className="doc-foundation-page__header mgb15">
        <h3 className="mgt0 mgb3 bold-font">{title}</h3>
        {description && (
          <p className="fg-text-secondary mgt0">{description}</p>
        )}
      </header>
      <div className="doc-foundation-page__content">
        {children}
      </div>
    </div>
  );
}
