import React from 'react'
import DocPanel from '../../../Components/Common/DocMain/DocPanel'
import { ConfigurationItems } from '../../../Constants/GettingStarted/ConfigurationItems'

export default function Configuration() {
  return (
    <DocPanel 
      Header="Configuration" 
      SubHeader="How to consume the ULS library (LESS), register the PrimeReact passthrough map, and keep imports/utilities clean." 
      features={ConfigurationItems}
    />
  )
}
