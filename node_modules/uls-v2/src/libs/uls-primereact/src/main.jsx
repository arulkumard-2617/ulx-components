import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { PrimeReactProvider } from 'primereact/api'
import { ThemeProvider } from './Context/ThemeContext'
import 'primeicons/primeicons.css'
import App from './App.jsx'
import passThrough from '../../../scripts/passthrough.js'

// for testing purposes only
// import passThrough from '../../../dist/js/passthrough.js';

// Import ULS Dynamic Gap Manager
// import '../../../../uls-styles/js/uls.js';

// import styles
// Note: ULS theme styles are loaded dynamically by ThemeContext
// import './styles/custom-theme.less'  // Disabled: Using pure dynamic CSS loading
import './styles/Docs/Docs.less'

const pt = {
  ...passThrough,
  ripple: {
    root: { className: 'uls-button-ink' },
    active: { className: 'uls-button-ink-active' }
  }
};

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <PrimeReactProvider value={{ unstyled: true, ripple: true, pt }}>
      <ThemeProvider>
        <App />
      </ThemeProvider>
    </PrimeReactProvider>
  </StrictMode>,
)
