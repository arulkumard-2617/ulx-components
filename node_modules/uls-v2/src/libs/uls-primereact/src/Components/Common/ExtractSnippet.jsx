export function ExtractSnippet(source, snippetName) {
    if (!source) return "";
  
    const re = new RegExp(
      `BEGIN-SNIPPET\\s+${snippetName}([\\s\\S]*?)END-SNIPPET`,
      "m"
    );
    const match = source.match(re);
    return match ? match[1].trim() : source.trim();
  }