import React, { useState } from 'react';
import { Slider } from 'primereact/slider';

export default function Range() {
    const [value, setValue] = useState([20,80]);
    
    return (
        <Slider value={value} onChange={(e) => setValue(e.value)} className="w300" range />
    );
}