import React, { useState } from 'react';
import { Rating } from 'primereact/rating';

export default function WithoutCancel() {
    const [value, setValue] = useState('');
    
    return (
        <Rating value={value} onChange={(e) => setValue(e.value)} cancel={false} />
    );
}