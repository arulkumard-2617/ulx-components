import React, { useState } from 'react';
import { Slider } from 'primereact/slider';

export default function Basic() {
    const [value, setValue] = useState(0);
    
    return (
        <Slider value={value} onChange={(e) => setValue(e.value)} className="w300" />
    );
}