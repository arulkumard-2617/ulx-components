import React, { useState } from 'react';
import { Rating } from 'primereact/rating';

export default function NumberofStars() {
    const [value, setValue] = useState(null);
    
    return (
        <Rating value={value} onChange={(e) => setValue(e.value)} stars={10} />
    );
}