import { useRef } from 'react';
import { Stepper } from 'primereact/stepper';
import { StepperPanel } from 'primereact/stepperpanel';

export default function HeaderPosition() {
    const stepperRef = useRef({});

    return (
        <>
            <h5 className="mgb5 font-bold">Position top</h5>
            <Stepper ref={(ref) => (stepperRef.current.top = ref)} headerPosition="top">
                <StepperPanel header="Header I"></StepperPanel>
                <StepperPanel header="Header II"></StepperPanel>
                <StepperPanel header="Header III"></StepperPanel>
            </Stepper>
            <h5 className="mgb5 font-bold">Position right</h5>
            <Stepper ref={(ref) => (stepperRef.current.right = ref)} headerPosition="right">
                <StepperPanel header="Header I"></StepperPanel>
                <StepperPanel header="Header II"></StepperPanel>
                <StepperPanel header="Header III"></StepperPanel>
            </Stepper>
            <h5 className="mgb5 font-bold">Position left</h5>
            <Stepper ref={(ref) => (stepperRef.current.left = ref)} headerPosition="left">
                <StepperPanel header="Header I"></StepperPanel>
                <StepperPanel header="Header II"></StepperPanel>
                <StepperPanel header="Header III"></StepperPanel>
            </Stepper>
            <h5 className="mgb5 font-bold">Position bottom</h5>
            <Stepper ref={(ref) => (stepperRef.current.bottom = ref)} headerPosition="bottom">
                <StepperPanel header="Header I"></StepperPanel>
                <StepperPanel header="Header II"></StepperPanel>
                <StepperPanel header="Header III"></StepperPanel>
            </Stepper>
        </>
    );

}