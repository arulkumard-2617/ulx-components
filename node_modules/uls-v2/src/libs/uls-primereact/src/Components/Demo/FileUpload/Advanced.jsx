import React, { useState } from 'react';
import { FileUpload } from 'primereact/fileupload';

export default function Advanced() {
    const [value, setValue] = useState('');
    
    return (
        <FileUpload name="demo[]" url={'/api/upload'} multiple accept="image/*" maxFileSize={1000000} emptyTemplate={<p className="mg0">Drag and drop files to here to upload.</p>} />
    );
}